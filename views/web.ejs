<%- include('partials/header') %>

<!-- Hero Section - Same Layout as Homepage with Blue Gradient -->
<section class="hero webapp-hero">
    <div class="container">
        <div class="hero-content">
            <div class="hero-text">
        
                <h1 style="color: white;">
                   Professional Websites That Work
                   
                </h1>
                <p class="hero-subtitle" style="color: white; opacity: 0.9;">
                   Custom-built and mobile-friendly websites
  designed to showcase your brand and grow your business 24/7.
                </p>

                <!-- Feature Pills -->
                <div class="feature-pills">
                    <div class="feature-pill">
                        <i class="fas fa-check-circle"></i>
                        <span>Mobile Responsive</span>
                    </div>
                    <div class="feature-pill">
                        <i class="fas fa-check-circle"></i>
                        <span>Fast & Secure</span>
                    </div>
                    <div class="feature-pill">
                        <i class="fas fa-check-circle"></i>
                        <span>SEO-Friendly</span>
                    </div>
                </div>

                <div class="hero-cta-group">
                    <a href="https://wa.me/60187806530?text=Hi%20WebCraft%20Solution!%20I'd%20like%20to%20discuss%20a%20website%20project" class="btn btn-white">
                        <i class="fab fa-whatsapp"></i>
                        Let's discuss
                    </a>
                    <a href="/#portfolio" class="btn btn-outline-white">
                        <i class="fas fa-briefcase"></i>
                        View Examples
                    </a>
                </div>
            </div>

            <div class="hero-image">
                <div class="hero-image-placeholder">
                    <picture>
                        <source srcset="/images/iconbut/web.webp" type="image/webp">
                        <img src="/images/iconbut/web.png" alt="Web Development">
                    </picture>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Section 1: Introduction with Image on Left -->
<section class="hero intro-section">
    <div class="container">
        <div class="hero-content">
            <div class="hero-image">
                <div class="hero-image-placeholder">
                    
                    <picture>
                        <source srcset="/images/iconbut/web2.webp" type="image/webp">
                        <img src="/images/iconbut/web2.png" alt="Web Application Examples">
                    </picture>
                </div>
            </div>

            

            <div class="hero-text">
                <div class="section-header">
                    <p class="section-subtitle">WHY YOUR BUSINESS NEEDS A WEBSITE</p>
                    <h2 class="section-title">Your Digital Storefront</h2>
                  
                    <p class="section-description" style="margin-top: 20px;">
                     Website is your business's online home — a digital space where
  customers can learn about your services, browse your offerings, and contact you       
  anytime, anywhere.   
                    </p>
                    <p class="section-description" style="margin-top: 20px;">
                      We specialize in building websites that look great, load fast, and       
  work flawlessly on phones, tablets, and desktops. From simple landing pages to        
  full e-commerce stores, every site is custom-built to match your brand and goals.  
                    </p>
                </div>
                 <p class="webapp-tagline">Your brand deserves a home online.</p>
            </div>
        </div>
    </div>
</section>

<!-- Section 2: Use Cases -->
<section class="why-choose-section">
    <div class="container">
        <div class="section-header">
            <p class="section-subtitle">COMMON USE CASES</p>
            <h2 class="section-title">What We Build</h2>
            <p class="section-description">
                We create websites that help businesses establish credibility, attract customers, and grow online.
            </p>
        </div>

        <div class="why-choose-grid">
            <div class="benefit-item">
                <div class="benefit-title">
                    <i class="fas fa-building"></i> Corporate & Business
                </div>
                <p class="benefit-description">
                    Professional company websites featuring service pages, team profiles, company history, client testimonials, and contact forms to establish credibility and attract new business opportunities.
                </p>
            </div>

            <div class="benefit-item">
                <div class="benefit-title">
                    <i class="fas fa-utensils"></i> Restaurant & Cafes
                </div>
                <p class="benefit-description">
                    Menu displays with photos, location maps, operating hours, photo galleries, reservation information, and seamless integration with popular food delivery platforms like GrabFood and Foodpanda.
                </p>
            </div>

            <div class="benefit-item">
                <div class="benefit-title">
                    <i class="fas fa-shopping-bag"></i> E-Commerce
                </div>
                <p class="benefit-description">
                    Online product catalogs with shopping cart, checkout forms, email or WhatsApp order notifications, payment integration, and beautiful product galleries for small to medium retail stores.
                </p>
            </div>

            <div class="benefit-item">
                <div class="benefit-title">
                    <i class="fas fa-camera"></i> Portfolio & Creative
                </div>
                <p class="benefit-description">
                    Showcase websites for photographers, graphic designers, artists, videographers, and creative professionals to display their work through galleries and project case studies.
                </p>
            </div>

            <div class="benefit-item">
                <div class="benefit-title">
                    <i class="fas fa-briefcase"></i> Service Businesses
                </div>
                <p class="benefit-description">
                    Professional websites for salons, clinics, consultants, lawyers, and service providers highlighting your expertise, services offered, pricing packages, customer reviews, and easy contact options.
                </p>
            </div>

            <div class="benefit-item">
                <div class="benefit-title">
                    <i class="fas fa-file-alt"></i> Landing Pages
                </div>
                <p class="benefit-description">
                    Focused single-page websites designed for product launches, promotional campaigns, lead generation, event registration, and special offers with clear calls-to-action that drive conversions.
                </p>
            </div>
        </div>
    </div>
</section>



<style>
/* Import Space Grotesk from Google Fonts */
@import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@700&display=swap');

/* Web App Hero - Blue Gradient Background Override */
.webapp-hero {
    background: linear-gradient(135deg, #3399ff, #0066cc, #003d7a);
}

/* ========================================
   TAGLINE STYLING - Statement Line (Style B) with Typewriter Animation
   ======================================== */
.webapp-tagline {
    font-family: 'Space Grotesk', sans-serif;
    font-size: 23px;
    font-weight: 700;
    color: #0066cc;
    letter-spacing: 0.5px;
    margin-top: 30px;
    line-height: 1.5;
    text-align: center;
    min-height: 35px; /* Prevent layout shift during typewriter */
}

/* Blinking cursor for typewriter effect */
.webapp-tagline.typing::after {
    content: '|';
    animation: blink 0.7s infinite;
    margin-left: 2px;
}

@keyframes blink {
    0%, 50% { opacity: 1; }
    51%, 100% { opacity: 0; }
}

/* Mobile responsive for tagline */
@media (max-width: 767px) {
    .webapp-tagline {
        font-size: 18px;
        letter-spacing: 0.3px;
        margin-top: 20px;
        min-height: 28px;
    }
}

/* Image Animation - Slide In + Float */
@keyframes slideInRight {
    from {
        opacity: 0;
        transform: translateX(100px);
    }
    to {
        opacity: 1;
        transform: translateX(0);
    }
}

@keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-15px); }
}

.webapp-hero .hero-image img {
    opacity: 1;
    animation: slideInRight 0.8s ease-out, float 3s ease-in-out 1s infinite;
}

/* Text Animations - Cascade Effect */
@keyframes fadeInLeft {
    from {
        opacity: 0;
        transform: translateX(-30px);
    }
    to {
        opacity: 1;
        transform: translateX(0);
    }
}

@keyframes slideUp {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* Hero Text Animation */
.webapp-hero .hero-text h1 {
    animation: fadeInLeft 0.8s ease-out;
}

.webapp-hero .hero-subtitle {
    animation: fadeInLeft 0.8s ease-out 0.2s both;
}

/* Feature Pills */
.feature-pills {
    display: flex;
    gap: 12px;
    margin: 24px 0;
    flex-wrap: wrap;
}

.feature-pill {
    display: flex;
    align-items: center;
    gap: 8px;
    background: rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(10px);
    padding: 8px 16px;
    border-radius: 20px;
    color: white;
    font-size: 13px;
    font-weight: 500;
    border: 1px solid rgba(255, 255, 255, 0.3);
    transition: all 0.3s ease;
}

/* Pill Slide Up Animation with Stagger */
.feature-pill:nth-child(1) {
    animation: slideUp 0.6s ease-out 0.4s both;
}

.feature-pill:nth-child(2) {
    animation: slideUp 0.6s ease-out 0.6s both;
}

.feature-pill:nth-child(3) {
    animation: slideUp 0.6s ease-out 0.8s both;
}

.feature-pill:hover {
    background: rgba(255, 255, 255, 0.3);
    transform: translateY(-2px);
}

.feature-pill i {
    color: var(--success-green);
    font-size: 14px;
}

/* Buttons Animation */
.webapp-hero .hero-cta-group {
    animation: fadeInLeft 0.8s ease-out 1s both;
}

/* White Outline Button */
.btn-outline-white {
    background: transparent;
    color: white;
    border: 2px solid white;
}

.btn-outline-white:hover {
    background: white;
    color: var(--primary-blue);
}

/* Mobile Responsive */
@media (max-width: 767px) {
    .feature-pills {
        justify-content: center;
        gap: 8px;
    }

    .feature-pill {
        font-size: 12px;
        padding: 6px 20px;
    }
}

/* ========================================
   INTRO SECTION - Image on Left
   ======================================== */

/* Solid gray background (same as Section 3) */
.intro-section {
    background: var(--gray-50);
}

/* Fix opacity issue - make image visible immediately */
.intro-section .hero-image img {
    opacity: 1;
    animation: slideInLeft 0.8s ease-out, float 3s ease-in-out 1s infinite;
}

@keyframes slideInLeft {
    from {
        opacity: 0;
        transform: translateX(-100px);
    }
    to {
        opacity: 1;
        transform: translateX(0);
    }
}

/* Text justification for neat edges */
.intro-section .section-description {
    text-align: justify;
    text-align-last: left;
}

/* Desktop only: Flip layout (image left, text right) */
@media (min-width: 1025px) {
    .intro-section .hero-content {
        flex-direction: row-reverse;
    }
}

/* Mobile: Keep normal order (image on top, text below) */
@media (max-width: 1024px) {
    .intro-section .hero-content {
        flex-direction: column;
    }

    .intro-section .hero-image {
        order: -1;  /* Image on top */
        margin-bottom: 40px;
    }
}



/* ========================================
   SCROLL ANIMATIONS FOR NEW SECTIONS
   ======================================== */

/* Keyframe for fade up animation */
@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* Section Headers - Hidden by default */
.section-header .section-subtitle {
    opacity: 0;
}

.section-header .section-title {
    opacity: 0;
}

.section-header .section-description {
    opacity: 0;
}

/* Section Headers - Animate when visible */
.section-header.animate .section-subtitle {
    animation: fadeInUp 0.6s ease-out forwards;
}

.section-header.animate .section-title {
    animation: fadeInUp 0.6s ease-out 0.15s both;
}

.section-header.animate .section-description {
    animation: fadeInUp 0.6s ease-out 0.3s both;
}

/* Benefit Items - Hidden by default */
.benefit-item {
    opacity: 0;
}

/* Benefit Items - Animate when visible */
.benefit-item.animate {
    animation: fadeInUp 0.6s ease-out forwards;
}
</style>

<script>
    // Scroll Animation Observer for webapp.ejs
    const observerOptions = {
        threshold: 0.1,
        rootMargin: '0px'
    };

    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.classList.add('animate');
                observer.unobserve(entry.target);
            }
        });
    }, observerOptions);

    // Typewriter Effect Function
    function typewriterEffect(element, text, speed = 60) {
        element.textContent = ''; // Clear existing text
        element.classList.add('typing');
        let index = 0;

        const type = () => {
            if (index < text.length) {
                element.textContent += text.charAt(index);
                index++;
                setTimeout(type, speed);
            } else {
                // Typing complete - remove cursor
                element.classList.remove('typing');
            }
        };

        type();
    }

    // Tagline Observer (separate for typewriter effect)
    const taglineObserver = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                const tagline = entry.target;
                const originalText = tagline.textContent;
                typewriterEffect(tagline, originalText, 60);
                taglineObserver.unobserve(tagline);
            }
        });
    }, observerOptions);

    // Wait for DOM to load
    document.addEventListener('DOMContentLoaded', function() {
        // Observe intro section header (inside hero-text)
        const introSectionHeader = document.querySelector('.intro-section .section-header');
        if (introSectionHeader) {
            observer.observe(introSectionHeader);
        }

        // Observe tagline for typewriter effect
        const tagline = document.querySelector('.intro-section .webapp-tagline');
        if (tagline) {
            taglineObserver.observe(tagline);
        }

        // Observe all other section headers
        const sectionHeaders = document.querySelectorAll('.why-choose-section .section-header, .services-section .section-header');
        sectionHeaders.forEach(header => {
            observer.observe(header);
        });

        // Observe benefit items with stagger
        const benefitItems = document.querySelectorAll('.benefit-item');
        benefitItems.forEach((item, index) => {
            item.style.animationDelay = `${index * 0.1}s`;
            observer.observe(item);
        });


    });
</script>

<%- include('partials/footer') %>                